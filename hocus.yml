image:
  buildContext: .
  file: hocus.Dockerfile
tasks:
  - name: First task
    command: sleep 0.01
    init: python3 hello-world.py
  - name: Second task
    command: echo "This executed during the prebuild 👷🏻‍♂️"
    init: echo "This executes every time the workspace is started 🏃🏻‍♀️"
vscode:
  # You can specify extensions you want in your workspace here
  extensions:
    - redhat.vscode-yaml
